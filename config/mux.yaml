ackermann_mux:
  ros__parameters:
    topics:
      navigation:
        topic   : drive
        timeout : 0.2
        priority: 100
      joystick:
        topic   : teleop
        timeout : 0.1
        priority: 10
      emergency:
        topic   : emergency_drive
        timeout : 0.05
        priority: 1000   # 🚨 MAX
